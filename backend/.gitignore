# Build artifacts
bin/
obj/

# IDE
.vs/
.vscode/
*.user

# Secrets — never commit real connection strings/credentials.
# (appsettings.Development.json's local Docker Postgres creds are
# throwaway/localhost-only, so that file itself is fine to commit.)
appsettings.*.local.json
